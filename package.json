{
  "name": "my-lambda-project",
  "version": "1.0.0",
  "description": "Sample Node.js Lambda deployed via CodeBuild",
  "main": "src/index.js",
  "scripts": {
    "build": "echo 'Build step placeholder'",
    "test": "jest",
    "package": "zip -r build.zip src node_modules package.json"
  },
  "dependencies": {
    "aws-sdk": "^2.1484.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
